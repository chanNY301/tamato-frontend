# 扩展图标

请在此目录放置以下尺寸的图标文件：

- `icon16.png` - 16x16 像素
- `icon48.png` - 48x48 像素  
- `icon128.png` - 128x128 像素

## 临时解决方案

如果暂时没有图标，可以：

1. 使用在线工具生成图标（如 https://www.favicon-generator.org/）
2. 使用项目 logo（`tamato-frontend/src/assets/logo.png`）转换为不同尺寸
3. 创建简单的占位图标

扩展在没有图标的情况下也能工作，但浏览器可能会显示默认图标。

